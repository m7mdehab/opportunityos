# W5.1 validation repair — feed query regression assertions

## Source lock

- Integration branch: `work/fr008-incremental-delivery`
- Integration order base: `04d4643`
- Feature branch: `work/fr008-w51-query-contract`
- Preserve the unchanged failing supplemental run in `W5.1-FEATURE-R2-test.txt`.

## Finding

The supplemental `storage.test_feed_regression` run reached an existing stale SQL-shape assertion. On the locked base, `build_feed_query` already case-folded qualification decisions with `lower(...)`; W5.1 also generalizes work-mode and country equality to case-insensitive multi-select `IN` predicates. The test still expects direct equality for all three. This repair updates only those SQL-string expectations to the actual projection-only case-insensitive SQL contract. It does not change query behavior.

## Allowed files

- `storage/test_feed_regression.py`
- `reports/evidence/FR-008/orders/W5.1-*-ORDER.md`
- `reports/evidence/FR-008/orders/W5.1-*-READINESS.md`
- W5.1 test/repository/diff/raw-output evidence files

Do not modify APIs, schema, migrations, other tests, the user-provided brief, or application data in this repair slice.

## Acceptance

1. Assert the case-insensitive SQL fragments for decision, work mode, and country while retaining projection-only/no-description assertions.
2. Run `python -m unittest storage.test_feed_query storage.test_feed_regression` and `python scripts/check_repository.py` on synthetic in-memory fixtures only.
3. Preserve the R2 failure unchanged, then commit/push the test-only remediation and repeat the checks after integration.
