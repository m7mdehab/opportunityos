# W3.4 R1 remediation order — tie-break fixture

## Trigger

The first focused storage acceptance run was red. Its complete output is preserved unchanged at `reports/evidence/FR-008/orders/W3.4-R1-test.txt`.

## Finding

`FeedQueryContractTest.test_composite_score_tie_break_is_opportunity_id` expected `opp-6` and `opp-z` to tie, but only inserted `opp-z`; `opp-6` is introduced by a different test's local fixture. The failure is an incomplete test setup, not evidence of a query ordering defect.

## Authorized remediation

Add the missing synthetic `opp-6` row to this test with the same priority, then rerun the exact focused storage acceptance into a new immutable R2 log. Do not alter the preserved R1 output. No production behavior change is authorized by this remediation.
