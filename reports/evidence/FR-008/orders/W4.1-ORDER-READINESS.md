# W4.1 order readiness — Founder tracker schema foundation

- Feature branch: `work/fr008-w41-tracker-schema`
- Integration base: `d5c0dee`
- Existing schema review confirmed one triage-state table and no Founder activity/detail tables. The existing state API uses legacy values, so the migration must remain additive and avoid vocab constraints or inferred timestamps.
- Existing storage already supports externalized artifacts through `artifact_cache`; the snapshot row will reference that content object and store only small indexed metadata.
- New tracker data is Founder-private. Browser access remains denied through RLS; no UI/API grants are added.
- Only local SQLite metadata tests and offline Alembic SQL rendering are authorized. No application database URL will be used.

**Readiness: READY TO IMPLEMENT THE ADDITIVE SCHEMA MIGRATION AND ORM MODELS.**
