# W7.4 readiness — Desktop/mobile accessibility and keyboard behavior

Decision: READY / ORDER-LOCKED

W7.3 is integrated and its report/evidence are pushed at baee1c3. The brief lists W7.4 as desktop/mobile accessibility and keyboard behavior. Section 10.6 requires a usable filter drawer at about 390px, chips that wrap or scroll, reachable primary triage actions, accessible stage changes without drag-and-drop, and no horizontal table dependency. Acceptance A-27 calls for the desktop and 390px critical path through filters/sort, tracker states, stage, follow-up, restore, and detail history.

The existing feed and keyboard shortcuts have desktop mock coverage, and the root feed has an axe check. There is no FR-008-specific 390px end-to-end pass covering open filters/detail/tracker states or the complete mobile critical path. This slice will add that coverage and fix any concrete responsive/accessibility issues it finds, without changing API behavior or matching/tracker semantics.

The review surface remains local mock only. Several hosted tracker APIs intentionally return static 501, and no safe staging deployment target is configured for this slice. Use synthetic fixtures only; no private Founder data or live/staging tests.

Feature source may begin from: the W7.4 order-lock commit on work/fr008-incremental-delivery.
