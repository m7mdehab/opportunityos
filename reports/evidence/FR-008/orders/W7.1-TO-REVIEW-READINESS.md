# W7.1 readiness — To Review inbox behavior

**Decision:** `READY / ORDER-LOCKED`

W6.7's report identifies W7.1 as the next slice. The current SQL feed query already excludes active tracker rows and submitted jobs, has explicit `include_tracked` opt-in, and allows expired snoozes to resurface. Existing SQLite coverage verifies part of that contract.

The browser mock is not yet equivalent: it excludes snoozed rows indefinitely and treats `localStorage` failures as successful writes after mutating memory. The main UI removes a card only after a successful action response, so the mock write failure can be corrected at the store/handler boundary without changing feed interaction design.

W7.1 will harden these cases and expand state-matrix evidence. Undo remains W7.2. Work is local-only; the user-supplied brief and operation manual remain unmodified and unstaged.

**Feature source may begin from:** `548bd68` on `work/fr008-incremental-delivery`.
