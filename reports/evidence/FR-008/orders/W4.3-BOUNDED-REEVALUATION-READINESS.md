# W4.3 readiness — bounded reevaluation batch selection

## Locked inputs reviewed

- Integration base `63ff668` includes W4.1 tracker schema and W4.2 normalized projection fields.
- The production worker queue persists jobs with lease recovery, fenced completion, retry limits, and PostgreSQL `SKIP LOCKED` claims.
- The current evaluation persistence path upserts in place by `(opportunity_id, truth_pack_hash)` and refreshes the matching projection for each evaluated opportunity.
- The current `evaluate_new` worker path loads all missing candidates at once; projection rebuild uses keyset paging but has no total-row cap. W4.3 therefore starts with a read-only bounded selector; changing or invoking those operational paths is outside this order.
- The selector can be tested with temporary SQLite and synthetic opportunity IDs, without touching application configuration or Founder data.

## Implementation plan

1. Add a small immutable batch result and a read-only selector that queries only opportunity IDs in deterministic keyset order.
2. Enforce a hard maximum of 100 IDs per call, reject ambiguous argument types, and use a single look-ahead ID to report continuation.
3. Add temporary-SQLite tests for page size, stable cursor resume, final/empty batches, and invalid arguments.
4. Run focused tests, repository integrity, and diff checks; then push the feature and repeat checks on the integration branch.

## Pre-execution gate and limits

- Real reevaluation remains **not ready**: no approved 150+ real Founder-reviewed job set/W3.5 report is present in this task, and no safe local application DB target is configured.
- No corpus rows will be scored, updated, queued, or projection-refreshed in this slice.
- No deployed surface changes, so Founder-testable remains 0%.
- Next implementation step after this selector is W4.4 projection rebuild/invariant verification on synthetic fixtures. Real W4.3 worker execution stays a separately authorized, calibrated operation.

**READY_TO_DISPATCH:** YES — for the bounded read-only selector only.
