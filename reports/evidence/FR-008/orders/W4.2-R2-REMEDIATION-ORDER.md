# W4.2 R2 remediation order — staged diff whitespace

## Preserved finding

The staged diff check recorded in `W4.2-R2-diff.txt` exited 2 because report lines 3 and 4 ended in two spaces for Markdown hard breaks. This is documentation formatting only; no product source or migration change is required.

## Allowed remediation

Remove the two trailing spaces from `W4.2-FEED-PROJECTION-REPORT.md`, preserve `W4.2-R2-diff.txt` byte-for-byte, stage the corrected report, and rerun `git diff --cached --check`. Keep the whitespace finding in this order and the raw-output archive; do not rewrite or relabel the red result.
