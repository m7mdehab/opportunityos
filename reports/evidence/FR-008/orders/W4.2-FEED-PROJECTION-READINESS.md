# W4.2 readiness — feed projection field population

## Locked inputs reviewed

- Integration source is locked at `ee5b9d5` on `work/fr008-incremental-delivery`; the feature branch starts from that exact commit.
- W4.1 migration `0015_fr008_tracker_foundation` is the current schema head. W4.2 will add only `0016_fr008_projection_fields`.
- `FeedProjectionRecord` already stores `title_family`, `fit_score`, and `priority_score`; the existing build service reads preference/confidence from the persisted evaluation-detail JSON only to compute Recommended priority.
- The opportunity model already stores `title_level`; the deterministic `normalize_title` helper returns both family and level from the title.
- Typed target-role assertions and tier assertions are projected in `TruthGraph`. A tier is usable only when it is explicit and unambiguous for the matching verified target-role subject.
- Existing projection tests use synthetic opportunities/evaluations and local SQLite. They can exercise this contract without profile or corpus data.

## Implementation plan

1. Add nullable `title_level`, `target_tier`, `preference_score`, and `confidence_score` columns and indexes to the ORM projection model and additive migration.
2. Normalize posting title during normal projection construction. Resolve tier strictly from the supplied synthetic/approved graph following the order's same-subject and ambiguity rules.
3. Validate independent score values as finite numeric values in the closed interval 0–100; treat unusable detail values as unknown (`NULL`). Preserve the W3.4 priority formula and feed ordering.
4. Add local synthetic tests for populated and missing data, unknowns, and legacy compatibility. Render migration SQL offline, then run focused and repository checks.
5. Commit/push the feature checkpoint, integrate only after green acceptance, record the exact evidence, and continue to the separate W4.3/W4.4 work without corpus refresh here.

## Safety and compatibility checks

- No access to Founder-private or raw CV/profile content is needed or allowed.
- No source/evaluation row update, bulk rebuild, corpus backfill, live/staging database connection, migration application, deployment, or production score change is in scope.
- Existing projection rows retain nullable values until a later bounded, separately ordered rebuild. The new version marker identifies only rows built by the updated path.
- No browser/API surface changes in this slice, so staging is not a useful test surface yet and Founder-testable remains 0%.

## Review status

Ready for source implementation under the attached FR-008 feature request and its incremental delivery protocol. No acceptance result is claimed by this readiness note.
