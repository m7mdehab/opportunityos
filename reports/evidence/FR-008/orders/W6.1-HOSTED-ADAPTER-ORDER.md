# W6.1 addendum — hosted tracker boundary

## Source lock

- Integration head when locked: `aed14ae`
- Feature branch: `work/fr008-w61-basic-triage`
- Commit and push this addendum before editing the hosted adapter.

## Scope

- In `web/app/api/[...path]/route.ts`, return an explicit 501 for hosted `GET /api/tracker` and `POST /api/opportunities/{id}/actions` requests until the hosted data surface can update current state and append one activity event atomically.
- Do not approximate the transaction with multiple PostgREST writes and do not make a provider request during this work.
- Surface tracker action errors in the existing detail drawer. A failed request must leave the selected state and Jobs / To Review list unchanged.
- Local FastAPI and synthetic mock behavior stay as locked in the W6.1 work order.

## Verification

- Run focused TypeScript, ESLint, mock Playwright, repository integrity, and diff checks.
- Review the hosted adapter branches statically; no hosted credentials, requests, database changes, or deployment.

**READY_TO_DISPATCH:** YES — for the explicit hosted boundary and local error display only.
