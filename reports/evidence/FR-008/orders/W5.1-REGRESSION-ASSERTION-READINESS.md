# W5.1 validation repair readiness

The R2 supplemental run fails because a static SQL assertion predates the current case-insensitive SQL implementation. The locked-base source has used `lower(qualification_decision) = casefolded_value`; W5.1 changes work mode and country to `lower(column) IN (...)` for multi-select handling. Only three expected SQL fragments need updating in the existing regression test.

This is executable without a configured database. The test uses temporary in-memory SQLite to compile query SQL; the repository checker is offline. No Founder data, SQL execution against the application database, score changes, schema work, or deployment is needed.

**READY_TO_DISPATCH:** YES — for the test-expectation repair only.
