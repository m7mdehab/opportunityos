# W4.4 readiness — projection rebuild verification

## Locked inputs reviewed

- Integration base `96f83bf` includes additive migration `0016_fr008_projection_fields`, refresh-time v2 population, and the W4.3 employment-only selector.
- `rebuild_feed_projection` is an existing keyset-batched maintenance path with per-batch commit support; this work will call it only from synthetic SQLite tests.
- Current projection joins are unique by `(opportunity_id, truth_pack_hash)`, and match evaluations are unique on the same key.
- The verifier can select only title, organization, content hash, score, version, timestamp, and ID columns. It needs no full job description or Founder truth content; an optional synthetic TruthGraph is used only to assert tier mapping.
- The 150+ real Founder-reviewed gold set and approved safe local application DB target are not available, so no real reevaluation or projection refresh is authorized by this order.

## Implementation plan

1. Add a read-only one-page verifier with a 100-row cap, keyset cursor, one-row look-ahead, bounded issue counts, and hashed tokens.
2. Validate shared identity/copy fields and W4.2 v2 title/score/tier fields while preserving legacy v1 nulls.
3. Use temporary SQLite fixtures to rebuild two or three projections, verify a clean page/resume, corrupt selected scalar values to prove issue detection, and assert the SQL query does not fetch descriptions/raw payload/rationale/search text.
4. Run focused tests and repository/diff checks, then push and integrate this non-user-visible checkpoint.

## Readiness certificate

- Database/resource target: temporary in-memory SQLite only.
- Network/hosted resources: none.
- Founder-private data: not needed and not accessed.
- Unresolved real-corpus gate: approved 150+ reviewed jobs plus passing W3.5 review and a safe local database target.
- Scope executable without that gate: yes, for synthetic rebuild and read-only verification only.

**READY_TO_DISPATCH:** YES — for synthetic verification only.
