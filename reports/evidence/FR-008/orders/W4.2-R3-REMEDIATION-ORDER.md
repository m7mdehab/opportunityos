# W4.2 R3 remediation order — raw diff artifact handling

## Preserved finding

The staged diff check recorded in the `W4.2-R3-diff-raw-output.zip` archive exited 2 because the staged standalone copy of the earlier red diff output contains the two trailing-space lines it reported. The substantive report fix is already applied.

## Allowed remediation

Keep the exact red diff output in its byte-preserving ZIP archive and remove the redundant standalone text copy from the staged product evidence. Preserve this result and rerun the staged diff check against the archive-based evidence set. No source changes are authorized by this finding.
