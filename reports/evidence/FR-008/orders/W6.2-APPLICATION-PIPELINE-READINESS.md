# W6.2 application pipeline readiness

**State:** `READY FOR LOCKED IMPLEMENTATION`

## Scope check

W6.1 already uses the existing canonical triage row and append-only activity-event row. The current state column accepts the pipeline labels needed by W6.2; tracker bucket state lists already include active stages and terminal outcomes. The feature can therefore be implemented without changing schema or running a migration.

## Testability

- State transitions and event history can be exercised on temporary in-memory SQLite, as in W6.1.
- Mock handlers and synthetic `MockStore` data support a browser-only stage-flow test.
- TypeScript, ESLint, Playwright, repository-integrity, and diff-check commands are available locally.
- The hosted adapter correctly returns 501 for tracker mutations until an atomic state-plus-event operation is implemented. W6.2 keeps this boundary and will not contact a provider.

## Boundaries and decision

No safe hosted/local PostgreSQL target is configured for runtime/RLS acceptance. The work order excludes migrations and live data. Do not represent W6.2 as Founder-testable or deploy it. `Founder-testable` remains 0%.

**Readiness decision:** proceed with the pushed W6.2 order and isolated feature worktree using temporary SQLite plus synthetic mock browser coverage.
