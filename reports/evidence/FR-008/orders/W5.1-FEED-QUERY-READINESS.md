# W5.1 readiness — SQL feed query contract

## Inputs reviewed

- Integration base `b064bcf` includes W4.2 persisted score/title/tier fields, W4.3's read-only selector, and W4.4's scalar-only projection verifier.
- Current `storage/feed_query.py` already applies truth scope, visibility, decision, fit range, posted lower bound, and single work-mode/country/title-family/source filters in SQL. It orders only by Recommended priority plus stable ID.
- `feed_projection` has persisted work mode, country/city/region, remote scope, employment type, seniority, title family/level/tier, score components, source, and posted date. Multiple §7 filter families have no projection backing yet and are intentionally excluded from this contract.
- `GET /api/opportunities` passes only a subset of `FeedQuerySpec`; repeated query parameters can expose supported multi-select values without changes to the projection schema.
- Founder-testable delivery remains 0%; this backend query slice does not alter the UI and is not a staging deployment.

## Execution plan

1. Extend `FeedQuerySpec` with supported persisted filter arrays, independent score ranges, date range, and a constrained sort key.
2. Build OR predicates within each array filter and combine filter families through SQL AND. Define explicit unknown handling for nullable columns and use the existing stored sentinel for unspecified enums.
3. Add deterministic SQL ordering for the first manual-review slice and wire repeatable query values through the list route. Preserve legacy aliases and all current exclusions.
4. Add synthetic SQLite coverage for composability, unknowns, bounds, pagination, SQL ordering, and invariants; compile representative SQL offline for PostgreSQL.
5. Run focused and repository checks; push feature and integration checkpoints with evidence.

## Readiness certificate

- Database target: temporary in-memory SQLite only.
- PostgreSQL validation: SQL compilation only; no connection.
- Founder-private data: not needed or accessed.
- Schema/migration/backfill/deployment: none authorized or needed.
- Scope executable without gold-set gate: yes; query-contract work does not reevaluate or change scores.

**READY_TO_DISPATCH:** YES — for the persisted SQL query contract only.
