# W3.4 feature readiness — Recommended ordering

- Feature branch: `work/fr008-w34-recommended-ordering`
- Locked integration source base: `f013d357619d0c6c5c1bfc62fa586b19c6318c14`
- Scope: existing priority column, deterministic Recommended composite, default eligibility/tracked exclusions, explicit overrides, list API component payload, local SQLite and synthetic API acceptance.
- No migration, corpus backfill, live database, private Founder data, hosted service, FR-007 state, deployment, merge, or PR was used.
- `python -m unittest storage.test_ranking storage.test_feed_query storage.test_feed_projection_service`: **21 passed** (`W3.4-R2-test.txt`). R1's test-fixture-only red output is preserved with its remediation order.
- `python -m unittest api.test_api`: **11 passed, 19 skipped** (`W3.4-R3-test.txt`). Database-dependent tests remained skipped; no database was contacted.
- Repository integrity and `git diff --check` passed (`W3.4-R3-repository.txt`, `W3.4-R3-diff.txt`).
- Recommendation components are visible separately; neutral ordering defaults do not fabricate values in the payload. Interim ordering bands are not gold-set calibrated.

**Readiness: READY TO COMMIT AND PUSH AS A FEATURE CHECKPOINT. Integration acceptance remains pending.**
