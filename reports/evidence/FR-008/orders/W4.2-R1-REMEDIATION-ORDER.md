# W4.2 R1 remediation order — projection field tests

## Preserved finding

The unchanged initial focused run is stored in `W4.2-R1-test.txt`: 17 tests ran, with one failure and four errors.

- Four synthetic TruthGraph fixtures declared target tiers that their evidence text did not support. The graph validator correctly rejected those fixtures. The fixture evidence will explicitly state its synthetic tier; no production graph or truth data is changed.
- The invalid-score test expected freshness `100` for a posting dated eight days before the projection date. The product freshness contract returns `80` after seven days. The expected test component will be corrected to `80`; the production ranking algorithm and freshness thresholds will not change.

## Allowed remediation

Test fixtures and expected values only. Keep `W4.2-R1-test.txt` byte-for-byte unchanged. Rerun the same focused tests and preserve the complete result in a separate W4.2 R2 output.
