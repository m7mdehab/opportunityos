# W4.3 bounded reevaluation report

**State:** `PARTIAL / REVIEWABLE / CORPUS REEVALUATION NOT RUN`

**Feature branch:** `work/fr008-w43-bounded-reevaluation`
**Locked integration base:** `63ff668`

## What changed

Added a read-only employment batch selector that queries only opportunity IDs, sorts by ID, uses an exclusive keyset cursor, and asks for one extra row to determine whether more work remains. Each call is capped at 100 IDs. Independent-work rows are excluded. The selector neither loads descriptions/raw payloads nor invokes scoring, writes, queue enqueue, or projection refresh.

## Evidence

- `python -m unittest storage.test_reevaluation_batch`: 8 tests passed. Coverage includes ordered cursor resume, final/empty page behavior, employment-only selection, an observed SQL query limited to IDs, the 100-item cap/look-ahead, non-mutation, and strict input validation.
- `python scripts/check_repository.py`: passed.
- Final staged `git diff --cached --check`: exit code 0, no output (`orders/W4.3-R2-diff.txt`).
- The 6-test initial output and final 8-test output plus repository output are preserved byte-for-byte in `orders/W4.3-R1-checks-raw-output.zip`.
- The real batch selector itself has no DB write path. Tests use only temporary in-memory SQLite fixtures.

## Boundary and next step

This is the bounded selection primitive, not an evaluation/backfill result. No application database was opened, no reevaluation job was enqueued, no scoring weights changed, and no corpus or projection rows were modified. The gate for real reevaluation remains closed until an approved set of at least 150 Founder-reviewed real jobs passes the W3.5 metric review and a safe local database target is available. Founder-testable remains 0%. Next, integrate this slice and proceed to W4.4 synthetic projection rebuild/invariant verification.
