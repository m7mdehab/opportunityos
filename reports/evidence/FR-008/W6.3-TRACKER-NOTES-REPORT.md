# W6.3 — Private tracker notes

**Status:** `PARTIAL / REVIEWABLE / NOT YET FOUNDER-TESTABLE`
**Checkpoint:** Isolated feature branch `work/fr008-w63-tracker-notes`; no deployment or Founder environment is available.

## What changed

- Added authenticated list/create/edit/archive API operations for notes on application-tracked opportunities. The service reuses the W4.1 tables and caller-owned transaction; it adds no migration.
- Each real note mutation writes one append-only activity event in the same transaction. Event metadata contains only the note ID. Idempotent retries and no-op edits create no duplicate event.
- Added a private-notes panel to the opportunity detail drawer for application-tracked rows, with loading, empty, and error states.
- Added synthetic mock note storage, CRUD behavior, and event history persistence. The hosted adapter returns 501 for note routes before reading hosted configuration or contacting a provider.
- Corrected the E2E lifecycle check to await the PATCH response before asserting the persisted event count. Its prior text assertion could match the edit textarea while the request was still in flight.

## Tests and evidence

- `python -m unittest api.test_tracker_notes_service api.test_tracker_service` — 16 passed.
- `npx next typegen` — passed.
- `npx tsc --noEmit` — passed.
- `npm run lint` — passed.
- `npm run test:e2e -- tests/e2e/fr008-tracker-triage.spec.ts tests/e2e/keyboard.spec.ts` — 7 passed, including note CRUD, no-op/idempotency behavior, refresh persistence, existing tracker flows, and keyboard flows.
- `python scripts/check_repository.py` — passed.
- `git diff --check` — passed.

Command transcripts are under `orders/W6.3-FEATURE-*-test.txt`, including the earlier failed E2E attempts and their clean remediation. Byte-preserved originals are in `orders/W6.3-FEATURE-raw-output.zip`; readable text copies are whitespace-normalized so committed diff checks remain clean. `W6.3-FEATURE-R7-test.txt` records the temporary diagnostic run; those diagnostics were removed before the clean R8 and full R9 runs.

## Limits

No hosted note writes, provider calls, configured/live database, migration, private Founder data, corpus, or deployment was used. The hosted adapter remains 501, so Founder-testable progress remains 0%. This is a local-only review checkpoint and is not terminal FR-008 acceptance.

## Next

Continue with W6.4 follow-up dates/reminders as a separate locked slice. Keep the current hosted write boundary closed until atomic note/event persistence is available.
