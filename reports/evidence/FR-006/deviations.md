# BRIEF-FR-006 — Deviations

Maintained as they happen, not reconstructed at the end. One row each in the report per
`docs/AGENT_EXECUTION_PROTOCOL.md` §8.

| # | What | Why | Authorised by | Consequence |
|---|---|---|---|---|
| 1 | Track 0's `F2` was dispatched to an implementer rather than executed by the Master. | The brief calls Track 0 "sequential, Master"; the protocol's §0 says the Master never implements. The protocol is the later authority and the brief itself says it executes under the protocol. | Master, reading protocol §0 over brief §2 Track 0. | `dev_env.py` is subject to the same tier-1/2/3 verification as every other deliverable instead of being self-certified by its author. |
| 2 | Brief nodes **E2** and **E3** were merged into a single work order `E23`. | Both write `docs/SOURCE_REGISTRY.yaml` and `docs/SOURCE_EVIDENCE.md`, and `E1` appends to the same two files concurrently. A three-way conflict in a source-policy file is the worst conflict in this repository to resolve, because a mis-merge silently changes what an agent is permitted to fetch. | Master. | One fewer parallel slot; one less chance of a policy file being mis-merged. Both nodes' acceptance rows survive unchanged. |
| 3 | Migration `0004` **retains** `founder_filter_settings` instead of replacing it with `founder_facets`. | The brief says `0004` "replaces `founder_filter_settings` with `founder_facets`". The ten policy filters carry a `hide` / `rank_only` / `label_only` mode vocabulary; facets carry `include` / `exclude` / `off`. These are different semantics, and claim A-18 (carried from FR-005 A-13) requires the filter vocabulary to keep working — including the Overseer's own Appendix-5 decision that `target_roles` defaults to `rank_only`, a mode facets cannot express. Collapsing the two tables would have deleted the vocabulary that the Overseer's decision is written in. | Master. | Two tables instead of one. `/api/filters` survives; `/api/facets` is new. The brief's stated intent — every extracted attribute is a facet with include/exclude/off and nothing hides by default — is delivered in full. |
| 4 | The whole of migration `0004` — including columns and tables that work orders A2, C1, C2, D2 and E4 will populate — is specified by the Master and written by work order **A1** alone. | The brief has five deliverables sharing one revision. Five implementers editing one migration file in five worktrees produces either a merge conflict or five revisions. | Master, per protocol §4 (the Master resolves integration). | A1 is the critical path. Later orders are told `0004` is frozen and that a missing column is a scope question, not a `0005`. |
| 5 | The Master dispatched wave-1 implementers into worktrees branched from `main`, not from the brief branch, so none of them had its work order file. | Master error: the harness creates an agent worktree from the repository's default branch, not from the session's current branch, and the Master did not check before dispatching. Caught within one Master turn by inspecting `git worktree list`. | Nobody. This is a defect. | Four running agents were sent a correction telling them to `git merge --no-edit feat/brief-fr-006-nothing-missed` and to reconcile any work done on a guess against the order. Any work an implementer did before the correction was done without the Master decisions the order carries, so its return is judged against the order and not against what it thought it was building. |
| 6 | Work order **F2** hit its 60-turn budget with no `scripts/dev_env.py` written — 63 tool uses spent entirely on exploration. | Budget overruns are recorded, not extended silently (protocol §1). | Master. Recorded here rather than absorbed. | The order was **narrowed** and the same agent resumed with its context intact: write the two files in one call each, do the `sys.executable` sweep, run the six acceptance rows, stop exploring. Nothing on the critical path waited — every other implementer creates its database with `psql` directly, which is why the order's fallback path exists. |
| 7 | Brief nodes **C1**, **C4** and **B4** were merged into a single work order `C1`. | C4's hidden-reasons audit queries the same facet and filter state C1 owns, and B4 is an exercise of the same filters. Three worktrees editing `api/filters.py` and `api/routes_api.py` is a guaranteed conflict for no parallelism gain. | Master. | One work order, all three sets of acceptance rows preserved verbatim (C1.1-C1.10). |
| 8 | Brief nodes **E4** and **F3** were merged into a single work order `E4F3`. | Both live in `worker/handlers.py` and `worker/scheduler.py`. Two worktrees editing the same two files conflict for no parallelism gain. | Master. | One work order; both nodes' acceptance rows preserved. |
| 9 | Work order **B3** hit its 60-turn budget with nothing written — 63 tool uses, all exploration. The second budget overrun of the brief, after F2. | Recorded, not extended silently (protocol §1). The Master's dispatch prompts did not impose turn discipline, and the work orders send an implementer through several files before it writes anything. | Master. The systemic half is a Master defect. | B3 was resumed with a narrowed order naming the exact files, the exact call count, and a hard cap of five further reads. **Every subsequent dispatch carries an explicit turn budget**: read no more than a stated number of files, then write. |
| 10 | Work order **A1** was split into three after its implementer exhausted its budget: `A1` (extraction path only), `A1M` (migration 0004, ORM, repository, backfill), `A1C` (fixture corpus and metrics). | The original order bundled a schema change, an extraction rewrite across ten adapters, a qualifier change, a network capture of 200+ payloads, and a metrics script into one 60-turn budget. Five other work orders were blocked behind the migration inside it. The order was too large — a Master defect, not an implementer failure. | Master. | The migration is now a separate, fully-specified, mechanical order that unblocks A2, C1, C2, D2 and E4. The field-name spellings are a written contract between the two orders and the Master reconciles at integration. |
| 11 | Work orders **B1** and **A1** both hit the 60-turn budget with substantial work **uncommitted**. | Sonnet implementers at high effort spend heavily on verification reads before committing. | Master. | Both were resumed with `git commit` as step 1 before anything else. FR-005 lost an implementer's finished work to exactly this. Every subsequent resume opens with a commit instruction. |
| 12 | An editable install of the project shadows the agent worktrees: running a plain script with `py -3.12 <script.py>` puts the script's own directory first on `sys.path`, so `import matching` can resolve to a **different worktree's** code. Work order B1 discovered this when its first acceptance print silently ran against stale, pre-edit code. | Pre-existing environment property, not caused by this brief. Found because an implementer noticed its own numbers were wrong and said so. | Nobody — this is a finding. | Every acceptance command in every work order uses `-m`, which puts the working directory first and is unaffected. The finding is propagated to all running implementers. A number produced by a direct script invocation in this repository cannot be trusted to have come from the code under test, and that includes numbers in earlier briefs' evidence. |
| 13 | The first baseline suite capture came back truncated, with 16 inline ERROR markers and **no `Ran N tests` line**. The Master dispatched a second capture while the first runner's suite was still executing in the background, and both ran against the shared `opportunityos_test` database at once — 17 concurrent connections. | Master error: an evidence-runner was re-dispatched without confirming the first had stopped. FR-005 lost ten minutes to the same class of shared-database contention and the lesson did not carry. | Nobody. This is a defect. | Neither capture is evidence. A run with no `Ran N tests` line is not a pass and was not read as one. The baseline is recaptured once the database drains, from a single runner, after an explicit connection check. |
| 14 | Eleven work orders specified acceptance commands in the form `py -3.12 -m unittest <package> -v`. That form does **not** recurse into submodules — it reports `Ran 0 tests` / `NO TESTS RAN`. | Master error in the order text. Found by work order F1, which ran the command, got zero tests, `git stash`ed its own changes to confirm the behaviour was pre-existing rather than its own regression, and substituted `discover -s <pkg> -p "test_*.py"` — reporting both the substitution and why. | Nobody. This is a defect. | All eleven orders were corrected in place to the `discover` form. The behaviour F1 showed is the reason a `Ran 0 tests` line must never be read as a pass — the same rule that caught FR-005's zero-test Playwright run. |
| 15 | Claim **A-1**'s expected result says `0 skipped`. Three tests skip on this host. | Two are `@unittest.skipIf(os.name == "nt")` POSIX zombie-process tests in `scripts/test_alpha.py`; the third is the filter-seed guard, which skips until `0004` lands. The host is Windows. | Nobody — the expected result was written before the observation and is not being rewritten. | A-1 will close `NOT_CLOSED` on the zero-skip clause with the three skips named, or PASS once the seed guard un-skips and the two POSIX tests are dispositioned. FR-005's report claimed 672 tests with `0 skipped`; if these POSIX-gated tests existed then, that figure needs re-checking, and the verifier is asked to. |
| 16 | Work order **A1M** refused two genuine Master instructions as suspected prompt injection, so the `target_roles` data migration did not ship with `0004`. | The instructions arrived mid-task as messages rather than as order text, and asked for something that *appeared* to reverse a safety decision recorded in `0003`'s own docstring. A1M read its order's "do not drop or alter `founder_filter_settings`" as covering a row update, refused, and said so plainly — accepting only the one independently verifiable technical fact in the message after checking it against its own tool output. | Nobody. **This is not a defect and A1M is not at fault.** An instruction arriving mid-task that seems to reverse a recorded safety decision is exactly what an agent should be suspicious of, and the same behaviour occurred once in FR-005. | The change was re-routed through a **committed work order** (`A1S-seed.md`) — the channel a subagent can actually trust, and the one the protocol prescribes (§2: "subagents receive the work order, not the brief"). The order states the provenance of the decision, why a row update is not a table alteration, and why it is arriving separately. The Master's error was using chat as an authority channel for a scope change. |
| 17 | The A1M order listed `founder_opportunity_views` as a **new** table. It already exists, created by `0002_match_evaluations` with a different shape — a surrogate `id` primary key rather than `opportunity_id`. | Master error in the order text; the Master did not check the existing schema before enumerating `0004`. | Nobody. This is a defect. | A1M did not create a duplicate and did not alter the existing table; it recorded the discrepancy in the migration's docstring and named it as an assumption. Work order E4F3 is told the real shape before dispatch, since its "new since you last looked" marker reads that table. |
| 18 | Council review #1 terminated on an `HTTP 529 Overloaded` API error before producing findings. | Transient upstream capacity, not a repository or agent fault. | Nobody. | Re-dispatched with the same requirement text. The retry also carries one clarification the first did not: proficiency-aware skill matching (work order B2) is not yet in the diff, so its absence is not a finding — but anything in the diff that would obstruct it is. |
| 19 | Integrating B1 and B3 broke `matching/test_gold_set.py::test_gold_set_benchmark_execution`. The gold set asserts a "Senior Distributed Systems Architect" posting scores **75-100** and is `QUALIFIED` against the shared `create_test_graph()` fixture; it now does not. | The gold set's bound was written under the old model, where `is_senior` was any title containing "lead" and there was no `title_family_fit` dimension. The shared fixture carries no employment dates, so the honest tenure model cannot compute seniority for it, and `domain_fit` was reweighted 0.10 -> 0.05 to make room for the new dimension. Found by work order A1, which ran the suite, attributed the failure to files frozen for it by checking `git log`, and refused to investigate or fix outside its scope. | Nobody. This is a real regression and the gold set is doing exactly what a gold set is for. | Routed to B1 with an explicit constraint: it may **complete** the fixture with real tenure evidence (an incomplete fixture made complete is legitimate) but may **not** move `min_expected_fit_score`, widen the range, or relax the expected decision. If a properly-dated fixture still scores below 75, that is a finding about the scoring model for the Master and council to see, and both outcomes are acceptable results — only a moved goalpost is not. The per-dimension decomposition before and after was required so the cause is attributable rather than asserted. |
| 20 | **Every employment fit score on the branch was computed over dimension weights summing to 1.05, not 1.00.** Work order B3 reported the rebalance as `domain_fit 0.10 -> 0.05`, `title_family_fit 0.05 (new)`, `Total 1.00`. That report was false. | B3 edited the `.get()` **fallbacks** in `matching/scorer.py` (`weights.get("domain", 0.05)`, `weights.get("title_family", 0.05)`) but not the policy default in `matching/models.py:282`, which still carries `"domain": 0.10` and no `"title_family"` key. The `domain` fallback therefore never fires; the `title_family` one does. Effective sum 0.35+0.20+0.15+0.10+0.10+0.05+0.05+0.05 = 1.05. | Nobody. This is a defect, and the report of it was wrong in a way that would have shipped. | Found by work order B1's per-dimension gold-set decomposition, which printed `domain_fit weight=0.1` next to `title_family_fit weight=0.05` and flagged the sum as a separate observation outside its own defect. **Verified independently by the Master in the source before routing.** Returned to B3 with `matching/models.py` authorised for that one change, plus a required test that asserts both weight dicts sum to exactly 1.0 **and** that every `weights.get(<key>)` in the scorer has its key present in the policy — so a future dimension cannot be added by relying on a fallback again. |
| 21 | Council review #1 was reassigned from `fable` to an independent `opus` reviewer after **three** consecutive `HTTP 529 Overloaded` failures. | Upstream capacity. The brief's routing table names `fable` for council reviews and AGENTS.md requires an ADR to change routing policy — but AGENTS.md's own Transactional Brief Execution section states that "independence may be satisfied by a genuinely separate approved model, tool, or session", which is the property a council review actually needs. The substitute session did not participate in any implementation. | Master, under the AGENTS.md independence clause rather than as a routing change. | Council #1's finding quality is not comparable to a `fable` review and the report says so. If the remaining three councils can run on `fable`, they will; each substitution is recorded here. The retry also carries two facts the first attempt did not: the weight defect being repaired, and the fixture-tenure discrepancy — so the reviewer judges the evidence rather than rediscovering it. |
| 22 | 52 connections had accumulated on the shared `opportunityos_test` database — 44 idle, 2 idle-in-transaction, 1 idle-in-transaction-aborted, and 5 queries active for 37 minutes. | Orphaned suite runs from the two abandoned baseline evidence-runners (deviation 13). Their processes outlived the agents that started them, and the idle-in-transaction sessions were holding locks the active queries were waiting on. | Nobody. Consequence of the Master's earlier double-dispatch. | 46 idle sessions terminated. This is the FR-005 lock-contention failure mode a second time, and the lesson is now explicit: an evidence-runner that starts a long suite in the background must be confirmed stopped before another is dispatched against the same database, because killing the agent does not kill the query. |
| 23 | The baseline suite run completed at last: `Ran 672 tests in 3006.183s` / `FAILED (failures=6, errors=53, skipped=2)`. It is **not** a valid health baseline. | The DROP and CREATE of `opportunityos_test` both failed on the contended sessions (deviation 13/22), so the run executed against an unmigrated, concurrently-used database: 22 of the errors are `relation "match_evaluations" does not exist` cascades. A second independent run under the same conditions reported 684 tests / 51 errors with the same first error. | Nobody. Consequence of the Master's double-dispatch. | **No clean pre-brief baseline was obtained, and the report says so rather than quoting these numbers as a before-figure.** Two facts from it are still usable because they are structural rather than environmental: the pre-brief test count is **672**, matching FR-005's figure, and **`skipped=2`**. |
| 24 | **FR-005's report claimed 672 tests with `0 skipped`. The skip count was wrong.** | The two skips are `scripts.test_alpha.TestProcessLifecycleHelpers.test_is_zombie_detects_an_unreaped_exited_child` and `test_pid_alive_treats_an_unreaped_zombie_as_not_alive`, both gated `skipIf(os.name == "nt")` with the message "zombie process state is POSIX-specific; Windows has no equivalent". They are present at the pre-brief head, so they skipped in FR-005 too. FR-005's A-1 expected `0 skipped` and the row was recorded PASS. | Nobody. This is a defect in a report the Master wrote, found by this brief's own baseline capture. | Recorded here as an erratum candidate for FR-005 and put to the verifier for independent confirmation. This brief's A-1 will close `NOT_CLOSED` on its own zero-skip clause with the skips named, rather than repeating the error. Two POSIX-only tests skipping on a Windows host is not a quality problem; **claiming zero skips when there were two is**, because it is the class of unchecked assertion FR-004's erratum and FR-005's A-9 were both about. |
| 25 | **Only one new read-allowed source produces rows: Hacker News, 192 rows.** The brief's E2 acceptance asks for at least eight, including a Reddit route. | Recon found the field almost entirely closed to automated reading. `reddit_forhire` returned 403 on its first request; `upwork` and `superprof` returned 403 on robots.txt; `remote_co`, `naukrigulf` and `ai_jobs_net` timed out; the rest (YC Work at a Startup, Working Nomads, JustRemote, Arc, Mostaql, Khamsat, Contra, PeoplePerHour, Toptal) allow crawling but publish no documented JSON or RSS job API, and every adapter in this codebase parses a structured response rather than scraped SPA HTML. LinkedIn's robots explicitly prohibits automated access. | Nobody. This is the honest number and it is reported as reached, not stretched. | 22 new registry entries with dated 2026-09-03 outcomes; 25 sources closed as `manual_only` with working deep links surfaced through `opportunity/manual_sources.py`. Claim A-23 closes **NOT_CLOSED** at 1 of 8. Under §8 and Appendix 3 a blocked source is a valid closure, but eight was the target and one is what exists. The six unprobed Reddit subreddits are labelled `http_403 (generalized)` in `docs/SOURCE_EVIDENCE.md` with the reason — an inferred outcome, said to be inferred, rather than a second request to a host that had already refused. |
| 26 | A pre-existing policy defect was found and fixed: `freelancer` was registered `automation.read: allowed`. | The brief states Freelancer "stays credential-gated", and a prior recon pass had left it read-allowed. No adapter was bound to it, so nothing had actually fetched it — but the registry is the authority the preflight consults, and it said the wrong thing. | Nobody. This is a safety finding. | Corrected to `disabled` / `manual_only`. This is the exact class of defect claim A-16 exists to catch, and it was caught by the test E23 wrote for A-16 rather than by inspection. |
| 27 | The weight defect (deviation 20) is repaired: `matching/models.py` now sets `"domain": 0.05` and adds `"title_family": 0.05`, summing to exactly 1.0000. The gold-set high-fit score moved **87.0 -> 83.5** as the 5% inflation came out, still inside the unmoved `[75.0, 100.0]` bound. | — | Master, authorising `matching/models.py` for that one change. | The repair carries an `ast`-scanned test that walks `_score_employment` and `_score_independent` for every `weights.get("<key>", ...)` literal and asserts the key is present in the corresponding policy dict — so a future dimension cannot be added by leaning on a fallback that never fires. That test, not the sum assertion, is what would have caught the original defect. |
| 28 | Work order B3 used 7 Read/Grep calls against a stated budget of 5, noticed mid-task, switched to Bash inspection, and **disclosed the overage unprompted** as "the defect it is rather than omitting it". | — | Nobody. Recorded because it was volunteered. | No consequence. Logged as the behaviour the project wants: an agent that reports its own limit breach without being asked is worth more than one that silently stays inside it. |
| 29 | **A1C truncated the corpus from 2,740 captured payloads to 540**, 40 per source, and **computed its "before" qualification distribution against an empty `TruthGraph`** rather than the founder-shaped pack. | The truncation was for repository size (34MB -> 6.5MB), positional by original API return order, and applied before any metric was computed — disclosed as a named assumption rather than found. The empty-graph before-figure was because no founder pack was in that order's scope. | Master accepts the truncation; the before-figure is **not** accepted as A-12's baseline. | The corpus is 540 payloads over 15 sources against a floor of 200, with the Cloudflare "Senior Customer Engineer" set present. But A1C's before-figure (qualified 6.1%, ineligible 75.0%, uncertain 18.9%) is **not comparable to the founder's observation** that every card said *Uncertain*: against an empty graph almost everything is `ineligible` because nothing can match. A-12's uncertain-share metric must be re-run against the **founder-shaped synthetic pack** post-integration, and the report quotes that figure, not this one. |
| 30 | **D1 could not meet four content thresholds and refused to fake any of them.** 5 of 7 non-internship roles rendered **1** bullet, not the required >= 2; projects rendered **no URLs**; skills could not be grouped "by the pack's categories"; and `dates_parsed_count` was 0 because dates render ISO rather than the brief's `"Jan 2026"`. | Every cause is fixture shape or schema, not the compiler. `truth/fixtures.py` gives most roles exactly **one** responsibility, so there is no second bullet to select. Neither pack sets `PortfolioItem.url`. **`SkillRecord` has no `category` field at all**, so the brief's requirement is unmeetable as written. Dates are ISO because the validator's lexical coverage requires a claim's words to appear in its evidence, and no pack's evidence text contains month names. | Master. | Work order **D1F** does the legitimate half: make the fixtures realistic (>= 2 bullets per role, portfolio URLs, skill categories and honest proficiencies including at least one `basic`), and add `SkillRecord.category` to the schema. The ISO date stands: rendering "Jan 2023" against evidence reading "2023-01-01" would require relaxing lexical coverage, which §9 makes a hard stop, and an ISO date is ATS-readable. **The report states both bullet counts — 2 of 7 before, and whatever D1F reaches — because a threshold met after the fixture was enriched is a different claim from one met as found.** |
| 31 | **There are two artifact validators, and D1's new predicates broke the legacy one.** `matching/validator.py` holds a second `ArtifactClaimValidator` with a **static claim-predicate whitelist**; the production path is `matching/artifact_validation.py` -> `truth/validator.py` under ADR-0014. `matching/test_adversarial.py::test_valid_compiled_cv_passes_validation` now fails. | D1 added eight new claim predicates for the rebuilt CV. It found the regression, attributed it, and declined to fix it because neither file was in its allowed list. | Nobody. The duplicate validator is pre-existing; the regression is new. | Routed to D1F to grep for callers and either extend the whitelist or **retire** the legacy validator, recording the decision as an ADR-0017 amendment. Explicitly forbidden: widening the whitelist to admit a predicate the production validator would reject. |
| 32 | **The generated CV carries no phone number.** Identity renders name, headline, email, LinkedIn, GitHub, website and location — seven items — but `identity.phone` is omitted from every document. | A bare digit run trips the validator's unverified-metric check, so the phone claim is rejected. This is a validator false positive on a legitimate identity value. | Nobody. This is a finding. | D1F investigates only whether the phone can render **without** any validator change; if not, it stays a recorded gap. Named here prominently because the founder's original complaint was "no name, no contact details", and this is the one contact field still missing. |
| 33 | The gold-set benchmark regressed **twice**, on two different dimensions, for the same underlying reason. | Each time a scoring dimension became honest, the shared `create_test_graph()` fixture turned out to lack the evidence that dimension needs: first employment dates (seniority fell to "no computable tenure"), then skill proficiency (B2's model correctly treats proficiency-less skills as partial, so `core_skills` fell from raw 1.0 to 0.5 and the total from 83.5 to 66.0). | Nobody. This is the gold set doing its job. | Both routed with the same constraint — complete the fixture, never move the bound — and both times the implementer complied and reported the decomposition. **The systemic finding is more useful than either fix: a benchmark fixture assembled against a lenient model silently under-specifies what an honest model requires.** B2 was asked to say whether anything else in that fixture is similarly thin, so the Master can decide whether the gold set needs rebuilding rather than patching. Recorded as a next-phase prerequisite either way. |
| 34 | Work order C1 implemented **15 facets — the brief's listed attributes only — and not "plus the existing ten"** as §2 C1 says. | C1 named it as an assumption rather than leaving it implicit. | Master, and it follows from deviation 3. | Consistent, not a loss: the ten policy filters keep their own surface at `/api/filters` with their `hide`/`rank_only`/`label_only` vocabulary, because that is the vocabulary the Overseer's Appendix-5 decision is written in. They are filters, not facets. Claim A-14 is judged against the 15 attributes plus the surviving ten filters, and the report says so plainly. |
| 35 | C1's 10% over-hiding warning is **tested as a pure function, not wired to an endpoint**, and the `language` facet is declared but permanently unavailable (422). | C1 disclosed both. No persisted data source for `language` exists anywhere in the schema. | Nobody. Recorded. | The brief requires the warning to be *visible*, so wiring it is added to work order C3's UI scope. The `language` facet returning an explicit 422 `unavailable` is better than a facet that silently returns everything, but claim A-14's "every attribute is a facet" closes at 14 of 15 available, with `language` named. |
| 36 | **E1 registered 36 boards against the brief's target of 300.** 702 candidates probed: live 42, empty 8, absent 651, blocked 0, error 1. | The sweep stopped on a session time budget, not on seed exhaustion or policy — 1,057 of 1,759 deduplicated candidates remain unprocessed and resume from a progress file. The dominant cost is that 651 of 702 probes were 404: the seed directory supplies company names, and guessing an ATS board token from a company slug is mostly wrong. | Nobody. 36 is the honest number and is reported as reached. | Claim A-23's board target closes **NOT_CLOSED at 36 of 300**, with the classification breakdown and the resumability in evidence. Two facts stop this being worse than it reads: the relevance filter used was the real `title_family_model`, not the committed keyword fallback, so the 36 are genuinely relevant; and the registry grew from 52 entries to 110 across E1 and E23 combined. |
| 37 | **Ashby stays `disabled`.** Re-recon of the documented posting-API host returned **HTTP 401 on robots.txt**, unchanged from FR-003's finding, dated 2026-09-03. | The brief authorised a re-check and a flip "if it permits". It does not permit. A secondary host (`jobs.ashbyhq.com`) returned 200 with a narrow disallow list, but it is not the posting-API host and is not authoritative for it. | Nobody. This is the rule working. | No `ashby:*` entry was modified and Ashby was excluded from the discovery sweep entirely. E1 did **not** flip it on the strength of a working endpoint, which is the temptation the brief's own wording ("if it permits, flip") invites. |
| 38 | Three `scripts/test_corpus_metrics.py` tests fail because they **asserted that the extraction metrics report `NOT AVAILABLE`** — a property that was true when A1C wrote them and false now that A1's fields exist. | A1C was dispatched before A1's fields landed and correctly refused to report zeros for attributes that did not exist. Its tests encoded that temporary state. | Nobody. This is the §1 "tests that pass only on the old behaviour" case. | Routed to work order **A12M** with an explicit instruction to *replace* rather than delete: the "reports NOT AVAILABLE rather than 0%" property is still worth testing, but by simulating a missing attribute rather than depending on one. A12M also carries claims **A-12** and **A-13**, both of which were unmeasured, and the requirement that the uncertain share be measured against the founder-shaped pack rather than A1C's empty graph. |
| 39 | The `reports/evidence/FR-006/baseline/` logs were **deleted**. | They tripped two guard rules (`PII_API_KEY_SHAPE`, `SECRET_OPENAI_KEY`) on fixture strings inside a 209KB captured test log, failing claim **A-4**. They were already ruled unusable as a health baseline (deviation 23) because the run executed against a contended, unmigrated database. | Master. | The two facts worth keeping from them are structural and are already recorded in deviations 23 and 24: the pre-brief suite is **672 tests with `skipped=2`**. Nothing else in those files was evidence for anything. Deleting an invalid artifact that also breaks the secret guard is strictly better than keeping it and weakening the guard, which is the direction §9 forbids. |
| 40 | **Three of the brief's four Track A/B thresholds are missed, two of them by a wide margin.** Measured over the committed 540-payload corpus: work mode `!= unspecified` **282/540 (52.2%)** against a 90% target; country-or-remote-scope **390/540 (72.2%)** against 85%; title-family mapping **111/540 (20.6%)** against 95%. The uncertain share is **100/540 (18.5%)** against a <25% target and is **MET**. | Real job-board data is less structured than the brief assumed. `work_mode_source` splits 21.7% adapter / 30.6% inference / **47.8% none** — nearly half the corpus states neither a work mode nor anything a rule can infer one from. The title-family gap is scope, not correctness: the corpus is fifteen whole-company job boards carrying sales, legal, finance, recruiting, design and support roles that the founder-targeted taxonomy never covered. | Nobody. These are the honest numbers and they are reported as measured. | Claims **A-12** and **A-13** close `NOT_CLOSED` with all four figures stated against their targets. The title-family gap is routed to B3 to broaden the taxonomy to the labour market rather than the founder's targets, on the reasoning that `title_family` is a **facet** and a facet that buckets four of five postings as `other` is not a facet — relevance to the founder is the scored dimension's job, not the taxonomy's. The extraction gap is not fixable inside this brief and becomes a next-phase prerequisite. |
| 41 | A12M found and fixed a **latent bug that had been inflating the extraction metrics**: `str(WorkMode.UNSPECIFIED)` renders as `"WorkMode.UNSPECIFIED"`, so the coverage comparisons against the plain string `"unspecified"` were always true. | Enum `__str__` versus the `str` mixin's `__eq__`. The comparison looked right and was not. | Nobody. This is a defect found while measuring. | Fixed by comparing the enum member directly. **The numbers in deviation 40 are the post-fix numbers**; the pre-fix ones would have looked better and been wrong. This is the second time in this brief that a measurement turned out to be measuring the wrong thing, after the empty-graph baseline. |
| 42 | The founder-shaped pack produces **0 core-skill strengths out of 540** corpus rows; 136/540 (25.2%) are partials. | The pack records 38 verified skill names and **zero** proficiency assertions. Under B2's model an absent proficiency is a partial, never a strength — which is correct behaviour against a pack that never says how good the founder is at anything. | Nobody. This is the model being conservative, and the pack being incomplete. | Work order D1F is setting honest, varied proficiencies on both synthetic packs, including at least one `basic` so the "Verified core skill: Javascript" path stays exercisable. Until then, the scoring evidence understates what the engine can do — and the report says so rather than quoting 0 as an engine property. |
| 43 | **Work order D1F closed the CV content gaps and the truth-lock still binds.** All **7 of 7** non-internship roles now render >= 2 bullets (was 2 of 7); portfolio URLs 2/2; six distinct skill categories; proficiencies set with honest variety (`working` 17, `advanced` 10, `foundations` 7, `expert` 3, `basic` 1); certifications 5. | — | Master, under work order D1F's bounded fixture-realism authorisation. | The row that mattered: **the guard-neutralisation mutation still fails two suites** after the enrichment, and restoring `truth/validator.py` leaves it byte-identical. Enriching the fixtures did not make the lock non-binding. The one `basic` skill (Power BI) renders as `"Nice-to-have: you have Power Bi (basic, 1 record)"` and never as a verified core skill, so B2's anti-regression holds against the real fixture rather than only against a constructed one. |
| 44 | The **legacy `matching/validator.py` was extended, not retired.** | The caller grep found two real callers outside its own tests — `outbound/authority.py:55` and `outbound/artifact_selector.py:16`, both default-constructing `ArtifactClaimValidator()`. Retiring it would have broken the outbound path. | Master, via D1F's decide-from-the-grep instruction. | Its whitelist now recognises the new claim predicates. This **never widens what `truth/validator.py` accepts** — the frozen production authority is unchanged; the shim simply stops rejecting predicate names it had not been taught. Recorded as an ADR-0017 amendment. Two validators remain a real duplication and it is a next-phase prerequisite, not something this brief resolves. |
| 45 | **`matching/ats_quality.py:97` contains literal backspace bytes (0x08) where `` word-boundary escapes were intended**, so `ats_parse_check` never matched a date and `dates_parsed_count` was always 0. | A `` written into an `r"..."` raw string as an actual control character rather than the two-character escape. Pre-existing, not introduced by this brief. | Nobody. This is a defect. | Found by D1F, which noticed the counter read 0 while independently confirming by substring check that the ISO dates were literally present in the extracted PDF text — then declined to fix it because the file was outside its allowed list. **Verified independently by the Master by scanning the file for 0x08 bytes before routing.** The fix carries a repository-wide scan for the same corruption, on the reasoning that this kind of damage arrives in batches, and a test that asserts the counter is non-zero for real input rather than asserting on the regex source. |
| 46 | **Council #3 found that the founder's entire existing database would have been invisible to the new search.** `0004` adds `search_tsv` nullable with no backfill, and `backfill_search_tsv` has no caller outside tests — so every row written before `0004` returns from no query. Not an error, not an empty result: absent. | The column was added correctly and the population path was written correctly; nothing connected the two for pre-existing rows. | Nobody. This is a defect, and it is the most consequential single finding of the brief. | Routed with the council's preferred fix — an `UPDATE ... WHERE search_tsv IS NULL` inside `upgrade()`, transactional with the column add — plus a test that a row inserted at `0003` is findable after upgrading to head. Found only because the council **ran** the migration against a fresh database and inspected the catalog rather than reading the diff. |
| 47 | The backup round-trip test seeds **zero rows** in all four new tables and sets none of the 17 new columns, so `_check_dump_row_counts` passes `0 == 0` whether or not the dump and restore loops work, and the base64 `payload` and `datetime.fromisoformat` restore paths never execute. | A test written to cover a schema rather than data. Its own comment explains why zero-row seeding is worthless for this class of bug. | Nobody. This is a defect. | Routed: seed one row per new table with a real `payload` and timestamps, plus one opportunity with every new column set, and assert row-for-row equality after restore. Backup completeness is a BRIEF-FR-003 invariant, and a test that cannot fail is worse than no test because it reads as coverage. |
| 48 | The ORM metadata declares **none** of `0004`'s five indexes, so `init_db()`'s `create_all` produces a schema **without the GIN index** and `alembic revision --autogenerate` would propose dropping all five. | The migration and the models drifted; only the migration knew about the indexes. | Nobody. | Routed. Council #3 also checked the `Text().with_variant(TSVECTOR(), "postgresql")` annotation specifically against the FR-002 fail-closed invariant and confirmed it is a type annotation introducing **no** SQLite fallback path — a question the Master had flagged as a risk at integration and would otherwise have carried into the report as an open doubt. |
| 49 | Migration `0004` is being **amended after other worktrees have already applied it**. | Council #3's finding 6 cannot be fixed any other way; the revision is unreleased to `main` but is live in several developer databases. | Master. | Recorded in advance rather than rediscovered: every database already at `0004` must be dropped and recreated. FR-005 lost time twice to exactly this when `0003` was amended in place, and the point of writing it down now is that the next person hits a note instead of a mystery. |
| 50 | **CORRECTION to deviation 25.** It recorded Hacker News as producing 192 rows. Council #4 established that the **bound** adapter's `feed_url` is the `whoishiring` *user* object, which `parse_payload` cannot read — so in production it yields **0 rows** and permanent schema-drift health. The 192 came from a manual smoke run of `fetch_who_is_hiring_payload`, a function not wired into the poll path. | The Master recorded a number from an implementer's smoke run as if it were a product capability, which is the FR-004 error in its original form. | Nobody. This is a Master defect in the record, found by the council and verified in the source before correcting. | The count of new read-allowed sources producing rows **in the product** is therefore **0**, not 1, until work order E5 wires the fetch path. `docs/SOURCE_EVIDENCE.md` must be corrected regardless of whether the wiring lands, and E5 is instructed that the correction is not contingent on the fix. |
| 51 | **CORRECTION to deviation 25.** It cited `recon/test_e23_transport_log.py` as proving no blocked source was retried. Council #4 found that test asserts a **hand-typed constant against itself**: the evidence file it names, `reports/evidence/FR-006/e23-recon-log.md`, does not exist, and the probe scripts it cites were never committed. | A test written to encode a claim rather than to check one. | Nobody. Master defect in the record. | The no-retry property may well hold — the recon narrative is consistent — but **it is not evidenced by that test**, and the report will not cite it as such. E5 must either commit the probe and its raw log or reword the test to say it is a manual transcription. |
| 52 | **An unpoliced fetch path.** `opportunity/adapters/hacker_news.py` calls `urllib.request.urlopen` directly, bypassing `validate_preflight`, `AcquisitionService` and every rate limiter — up to 261 unpaced GETs — and never consults `is_read_allowed`, so setting the registry entry to `disabled` **would not stop it**. A 403 mid-loop raises uncaught and records nothing. | The adapter was written as a multi-step Firebase fetch outside the single-request pattern every other adapter follows, and nothing forced it back through the gate. | Nobody. This is the most serious defect of the brief. | Verified by the Master in the source (raw `urlopen` at line 83) before routing. E5 fixes it and adds the test that matters: flipping the registry to `disabled` must make the fetch **refuse**. "Coverage is not permission" means the registry is the authority the fetch path consults — a path that does not consult it is outside the policy system entirely, however well-behaved it happens to be today. |
| 53 | **The registry asserts a rate-limit control the code does not implement.** 36 generated entries carry `rate_limits.documented: shared_per_ats_host`; `AcquisitionService` paces per `source_id`, and `worker/handlers.py` builds a fresh pipeline and limiter per job, so nothing is shared even per board across jobs. | The template was written to describe the intended control; the sweep implements it, the production poll path does not. | Nobody. | E5 must make the two agree — preferably by keying the limiter on the host and holding one process-wide instance, otherwise by weakening the template's claim. A policy file describing a control that does not exist is worse than one that admits the gap, because the gap is then invisible to everyone including the next auditor. |
| 54 | Three further fail-open policy paths, all found by council #4: a founder watchlist entry with `kind: ashby` would make the sweep probe a source registered **disabled** on a 401 and write it back as `read: allowed`; a corrupt discovery progress file returns `{}` and silently re-probes previously **blocked** boards; and a 403/429 sets health status but the scheduler re-enqueues the source after its interval with **no gate**, so a blocked source is re-requested on a later tick. | Each is a default that fails toward fetching rather than toward refusing. | Nobody. | All routed to E5. The session-scoped never-retry rule was honoured throughout this brief; these are the across-session and across-crash holes in it, and they were invisible until someone enumerated the code paths rather than the outcomes. |
| 55 | Council #4 confirmed **six** properties as genuinely satisfied by offline enumeration of all 110 registry entries and all 20 bound adapters: every entry has `prepare` and `submit` disabled; `docs/AGENT_PERMISSIONS.yaml` is **byte-identical** to baseline; no adapter binds to a `manual_only` or `disabled` source; User-Agents are truthful and non-browser (`OpportunityOS-Recon/1.0`); `manual_only` deep links are string-templated with no fetch path; and the sweep's limiter is keyed by ATS kind. | — | — | Recorded because a review that only lists defects tells the reader nothing about what was checked and found sound. These six are the core of claim **A-16** and they hold. |
| 56 | **Wave-3 integration: `Ran 958 tests`, `FAILED (failures=1, skipped=3)`.** A-0 fail-closed `Ran 12 tests / OK`; guard and repository checks both exit 0; `alembic upgrade head` reaches `0004_founder_control`. | — | — | 958 against a pre-brief baseline of **672**. The single failure is a deliberate tripwire (deviation 57). Of the three skips, two are the POSIX zombie tests and the third is the 20k search-timing run, gated behind `OPPORTUNITYOS_RUN_SEARCH_PERF=1` — which means **claim A-15's p95 figure is not evidenced by a normal suite run** and the report must say the number came from a gated invocation. |
| 57 | The only failing test in the 958 is one C2 wrote **to fail on purpose**: `PytorchCorpusSearchTest` asserts `opportunity/fixtures/corpus/ now exists on this branch -- this test must be rewritten to load it instead of the synthetic rows`. | C2 built its search evidence against synthetic rows because the corpus had not landed on its base, and rather than leave a synthetic result looking like a real one, it planted a tripwire that fires the moment the real data arrives. | Nobody. This is the design working. | Routed back to C2 to rewrite against the real 540-payload corpus, with the explicit instruction that **if no corpus row mentions pytorch the answer is 0** — a fact about fifteen job boards, not a search failure — and that it must not be disguised by loosening the query or seeding a synthetic row beside the real ones. This is the same self-invalidating-allowlist pattern FR-005 used for orphan predicates, and it is the second time it has paid for itself. |
